# 🚀 Netlify 部署指南 - 5分钟搞定！

## 🎯 为什么选择Netlify？
- ✅ **完全免费**
- ✅ **拖拽部署** - 不需要命令行
- ✅ **自动HTTPS** - 安全访问
- ✅ **全球CDN** - 访问速度快
- ✅ **自定义域名** - 可以绑定你的域名

## 📋 部署步骤

### 第1步：构建你的网站
在本地运行以下命令：
```bash
cd personal-story-tracker/frontend
npm install
npm run build
```

这会在 `frontend` 目录下生成一个 `build` 文件夹。

### 第2步：注册Netlify
1. 访问：https://netlify.com
2. 点击 **"Sign up"**
3. 选择 **"GitHub"** 登录（推荐）
4. 授权Netlify访问你的GitHub

### 第3步：部署网站
1. 在Netlify首页，找到 **"Want to deploy a new site without connecting to Git?"**
2. 点击 **"Browse to upload"**
3. 选择你刚才生成的 `build` 文件夹
4. 拖拽整个文件夹到页面上
5. 等待上传完成（约30秒）

### 第4步：获取网址
- 部署完成后，Netlify会给你一个网址
- 格式类似：`https://amazing-name-123456.netlify.app`
- 这就是你的网站地址！

## 🎨 自定义设置

### 更改网站名称
1. 在Netlify控制台，点击 **"Site settings"**
2. 点击 **"Change site name"**
3. 输入你想要的名称（如：`yixuan-story-tracker`）
4. 新网址：`https://yixuan-story-tracker.netlify.app`

### 绑定自定义域名（可选）
1. 在 **"Domain settings"** 中
2. 点击 **"Add custom domain"**
3. 输入你的域名
4. 按照提示配置DNS

## 🔄 自动部署（进阶）

如果你想要每次更新代码都自动部署：

1. 在Netlify中点击 **"New site from Git"**
2. 选择GitHub，授权访问
3. 选择你的 `personal-story-tracker` 仓库
4. 设置构建命令：
   - **Build command**: `cd frontend && npm run build`
   - **Publish directory**: `frontend/build`
5. 点击 **"Deploy site"**

以后每次你推送代码到GitHub，网站就会自动更新！

## 🎉 完成！

你的网站现在已经在线了！任何人都可以通过网址访问。

**示例网址**：https://yixuan-story-tracker.netlify.app

---

**💡 提示**：Netlify是很多开发者的首选，简单、稳定、免费！